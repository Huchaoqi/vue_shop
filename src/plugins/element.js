import Vue from 'vue'

import { Button, PageHeader, Form, FormItem, Input, Message } from 'element-ui'

Vue.use(Button)
Vue.use(PageHeader)
Vue.use(Form)
Vue.use(FormItem)
Vue.use(Input)
Vue.prototype.$message = Message
